{
  "name": "solasakicomics",
  "version": "1.0.0",
  "dependencies": {
    "resend": "latest"
  },
  "scripts": {
    "generate": "node scripts/generate-list.js",
    "build": "npm run generate"
  }
}
